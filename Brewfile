# Brewfile — install everything with:  brew bundle --file=~/dotfiles/Brewfile
# The installer runs this automatically if it's present in your repo.

# ── Taps ─────────────────────────────────────────────
tap "FelixKratz/formulae"          # sketchybar
tap "BarutSRB/tap"                 # omniwm

# ── CLI tools ────────────────────────────────────────
brew "stow"                        # symlink manager (required by the installer)
brew "git"
brew "eza"                         # your `ls` replacement
brew "zoxide"                      # smarter cd
brew "starship"                    # prompt
brew "neovim"
brew "vim"
brew "nushell"
brew "fzf"                         # fuzzy finder (nvim / shell)
brew "ripgrep"                     # fast search (nvim)
brew "fd"                          # fast find (nvim)
brew "sketchybar"                  # menu bar
brew "omniwm"                      # tiling window manager

# common extras — uncomment what you use
# brew "node"
# brew "go"
# brew "python"

# ── GUI apps ─────────────────────────────────────────
cask "wezterm"
cask "ghostty"

# add your other apps here, e.g.:
# cask "visual-studio-code"
# cask "cursor"
# cask "docker"
# cask "postman"
# cask "ollama"

# ── Fonts ────────────────────────────────────────────
cask "font-meslo-lg-nerd-font"     # Powerlevel10k prompt + Nerd Font glyphs
